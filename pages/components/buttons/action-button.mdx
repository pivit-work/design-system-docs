import { Button } from '../../../components/Button'
import { ComponentPreview, PropsTable, StateLabel } from '../../../components/ComponentPreview'

# Action Button

가장 일반적으로 사용되는 버튼 컴포넌트입니다.
사용자의 주요 액션을 유도하거나, 폼 제출, 다이얼로그 확인 등에 사용합니다.

Figma 컴포넌트: `Buttons/Button`

---

## Hierarchy

버튼의 중요도에 따라 5가지 Hierarchy를 제공합니다.

<ComponentPreview code={`<Button variant="primary">Primary</Button>
<Button variant="secondary">Secondary</Button>
<Button variant="tertiary">Tertiary</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>`}>
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="tertiary">Tertiary</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="ghost">Ghost</Button>
</ComponentPreview>

| Hierarchy | 용도 |
| --- | --- |
| **Primary** | 화면에서 가장 중요한 액션. 한 화면에 1개 권장 |
| **Secondary** | 브랜드 컬러 배경의 보조 액션 |
| **Tertiary** | 낮은 우선순위 액션 |
| **Outline** | 테두리가 있는 중립 톤 버튼. 보조적 액션에 사용 |
| **Ghost** | 배경 없는 최소 스타일 버튼. 인라인 또는 반복 액션에 사용 |

---

## Size

4가지 사이즈를 지원합니다.

<ComponentPreview code={`<Button variant="primary" size="sm">SM (26px)</Button>
<Button variant="primary" size="md">MD (36px)</Button>
<Button variant="primary" size="lg">LG (44px)</Button>
<Button variant="primary" size="xl">XL (48px)</Button>`}>
  <Button variant="primary" size="sm">SM (26px)</Button>
  <Button variant="primary" size="md">MD (36px)</Button>
  <Button variant="primary" size="lg">LG (44px)</Button>
  <Button variant="primary" size="xl">XL (48px)</Button>
</ComponentPreview>

---

## States

각 Hierarchy별로 5가지 상태를 가집니다.

### Primary States

<ComponentPreview code={`<Button variant="primary">Default</Button>
<Button variant="primary" state="hover">Hover</Button>
<Button variant="primary" state="focused">Focused</Button>
<Button variant="primary" state="disabled">Disabled</Button>
<Button variant="primary" isLoading={true}>Loading</Button>`}>
  <Button variant="primary">Default</Button>
  <Button variant="primary" state="hover">Hover</Button>
  <Button variant="primary" state="focused">Focused</Button>
  <Button variant="primary" state="disabled">Disabled</Button>
  <Button variant="primary" isLoading={true}>Loading</Button>
</ComponentPreview>

### Secondary States

<ComponentPreview code={`<Button variant="secondary">Default</Button>
<Button variant="secondary" state="hover">Hover</Button>
<Button variant="secondary" state="focused">Focused</Button>
<Button variant="secondary" state="disabled">Disabled</Button>
<Button variant="secondary" isLoading={true}>Loading</Button>`}>
  <Button variant="secondary">Default</Button>
  <Button variant="secondary" state="hover">Hover</Button>
  <Button variant="secondary" state="focused">Focused</Button>
  <Button variant="secondary" state="disabled">Disabled</Button>
  <Button variant="secondary" isLoading={true}>Loading</Button>
</ComponentPreview>

### Tertiary States

<ComponentPreview code={`<Button variant="tertiary">Default</Button>
<Button variant="tertiary" state="hover">Hover</Button>
<Button variant="tertiary" state="focused">Focused</Button>
<Button variant="tertiary" state="disabled">Disabled</Button>
<Button variant="tertiary" isLoading={true}>Loading</Button>`}>
  <Button variant="tertiary">Default</Button>
  <Button variant="tertiary" state="hover">Hover</Button>
  <Button variant="tertiary" state="focused">Focused</Button>
  <Button variant="tertiary" state="disabled">Disabled</Button>
  <Button variant="tertiary" isLoading={true}>Loading</Button>
</ComponentPreview>

### Outline States

<ComponentPreview code={`<Button variant="outline">Default</Button>
<Button variant="outline" state="hover">Hover</Button>
<Button variant="outline" state="focused">Focused</Button>
<Button variant="outline" state="disabled">Disabled</Button>
<Button variant="outline" isLoading={true}>Loading</Button>`}>
  <Button variant="outline">Default</Button>
  <Button variant="outline" state="hover">Hover</Button>
  <Button variant="outline" state="focused">Focused</Button>
  <Button variant="outline" state="disabled">Disabled</Button>
  <Button variant="outline" isLoading={true}>Loading</Button>
</ComponentPreview>

### Ghost States

<ComponentPreview code={`<Button variant="ghost">Default</Button>
<Button variant="ghost" state="hover">Hover</Button>
<Button variant="ghost" state="focused">Focused</Button>
<Button variant="ghost" state="disabled">Disabled</Button>
<Button variant="ghost" isLoading={true}>Loading</Button>`}>
  <Button variant="ghost">Default</Button>
  <Button variant="ghost" state="hover">Hover</Button>
  <Button variant="ghost" state="focused">Focused</Button>
  <Button variant="ghost" state="disabled">Disabled</Button>
  <Button variant="ghost" isLoading={true}>Loading</Button>
</ComponentPreview>

---

## Icon Only

아이콘만 사용하는 버튼입니다.

<ComponentPreview code={`<Button variant="primary" size="sm" iconOnly={true}>+</Button>
<Button variant="primary" size="md" iconOnly={true}>+</Button>
<Button variant="primary" size="lg" iconOnly={true}>+</Button>
<Button variant="secondary" size="md" iconOnly={true}>+</Button>
<Button variant="tertiary" size="md" iconOnly={true}>+</Button>
<Button variant="outline" size="md" iconOnly={true}>+</Button>
<Button variant="ghost" size="md" iconOnly={true}>+</Button>`}>
  <Button variant="primary" size="sm" iconOnly={true}>+</Button>
  <Button variant="primary" size="md" iconOnly={true}>+</Button>
  <Button variant="primary" size="lg" iconOnly={true}>+</Button>
  <Button variant="secondary" size="md" iconOnly={true}>+</Button>
  <Button variant="tertiary" size="md" iconOnly={true}>+</Button>
  <Button variant="outline" size="md" iconOnly={true}>+</Button>
  <Button variant="ghost" size="md" iconOnly={true}>+</Button>
</ComponentPreview>

---

## Props

<PropsTable data={[
  { name: 'variant', type: "primary | secondary | tertiary | outline | ghost", default: "primary", description: '버튼의 Hierarchy (시각적 중요도)' },
  { name: 'size', type: "sm | md | lg | xl", default: "md", description: '버튼 크기' },
  { name: 'state', type: "default | hover | focused | disabled", default: "default", description: '버튼 상태 (문서 시각화용)' },
  { name: 'disabled', type: 'boolean', default: 'false', description: '비활성화 여부' },
  { name: 'isLoading', type: 'boolean', default: 'false', description: '로딩 상태' },
  { name: 'iconOnly', type: 'boolean', default: 'false', description: '아이콘 전용 모드' },
  { name: 'leftIcon', type: 'ReactNode', default: '-', description: '텍스트 왼쪽에 표시할 아이콘' },
  { name: 'rightIcon', type: 'ReactNode', default: '-', description: '텍스트 오른쪽에 표시할 아이콘' },
  { name: 'children', type: 'ReactNode', default: '-', description: '버튼 레이블 텍스트' },
]} />

---

## Figma Variants

| Property | Values |
| --- | --- |
| **Size** | `sm` `md` `lg` `xl` |
| **Hierarchy** | `Primary` `Secondary` `Tertiary` `Outline` `Ghost` |
| **State** | `Default` `Hover` `Focused` `Disabled` `Loading` |
| **Icon only** | `True` `False` |
